import 'package:flutter/material.dart';

class AbsenSiswaPage extends StatelessWidget {
  const AbsenSiswaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Absen Siswa')),
      body: const Center(child: Text('Halaman Absen Siswa')),
    );
  }
}
